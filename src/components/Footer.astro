---
import { getLangFromUrl, useTranslations } from '../utils/i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const currentYear = new Date().getFullYear();
---

<footer class="footer">
  <div class="footer-container">
    <div class="footer-content">
      <div class="footer-brand">
        <span class="brand-icon">🤖</span>
        <span class="brand-text">AI Blog</span>
      </div>
      
      <div class="footer-links">
        <a href={lang === 'en' ? '/' : `/${lang}/`}>Home</a>
        <a href={lang === 'en' ? '/blog/' : `/${lang}/blog/`}>Blog</a>
        <a href={lang === 'en' ? '/about/' : `/${lang}/about/`}>About</a>
        <a href="/rss.xml" target="_blank" rel="noopener">RSS</a>
      </div>
    </div>
    
    <div class="footer-bottom">
      <p class="copyright">
        © {currentYear} AI Blog. All rights reserved.
      </p>
      <p class="powered-by">
        {t('footer.poweredBy')} 
        <a href="https://astro.build" target="_blank" rel="noopener" class="astro-link">Astro</a>
      </p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--color-gray-900);
    color: var(--color-gray-100);
    margin-top: auto;
    padding: 2rem 0 1rem;
  }

  .footer-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--color-gray-700);
  }

  .footer-brand {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 700;
    font-size: 1.125rem;
  }

  .brand-icon {
    font-size: 1.25rem;
  }

  .footer-links {
    display: flex;
    gap: 2rem;
  }

  .footer-links a {
    color: var(--color-gray-300);
    text-decoration: none;
    font-size: 0.9rem;
    transition: color 0.2s ease;
  }

  .footer-links a:hover {
    color: var(--color-primary);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: 1.5rem;
    font-size: 0.875rem;
    color: var(--color-gray-400);
  }

  .astro-link {
    color: var(--color-primary);
    text-decoration: none;
    font-weight: 600;
  }

  .astro-link:hover {
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .footer-content {
      flex-direction: column;
      gap: 1rem;
      text-align: center;
    }

    .footer-links {
      gap: 1rem;
    }

    .footer-bottom {
      flex-direction: column;
      gap: 0.5rem;
      text-align: center;
    }

    .footer-container {
      padding: 0 1rem;
    }
  }
</style>